import { useRouter, type Href } from 'expo-router'; import { FileUp, FolderPlus, ImagePlus, MessageSquarePlus } from 'lucide-react-native'; import { View } from 'react-native';
import { PageScreen } from '@/components/page-screen'; import { SettingRow } from '@/components/setting-row'; import { useAppTheme } from '@/contexts/theme-context';
export default function CreateScreen(){const router=useRouter();const {colors}=useAppTheme();return <PageScreen title="Create" subtitle="Start something new with Jela"><View style={{gap:10}}><SettingRow icon={<MessageSquarePlus color={colors.primary}/>} title="New Chat" description="Ask, plan or think through anything" onPress={()=>router.replace('/(user)')}/><SettingRow icon={<FolderPlus color={colors.accent}/>} title="New Project" description="Keep related chats, files and context together" onPress={()=>router.push('/(user)/new-project' as Href)}/><SettingRow icon={<FileUp color={colors.success}/>} title="Upload File" description="Add a persistent workspace text file" onPress={()=>router.push('/(user)/files' as Href)}/><SettingRow icon={<ImagePlus color={colors.primary}/>} title="Create Image" description="Open Jela image creation mode" onPress={()=>router.replace({pathname:'/(user)',params:{compose:'image'}})}/></View></PageScreen>}
