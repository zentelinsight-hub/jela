import { useLocalSearchParams, useRouter } from 'expo-router'; import { useEffect, useState } from 'react'; import { View } from 'react-native';
import { AppText } from '@/components/app-text'; import { Button } from '@/components/button'; import { LoadingState } from '@/components/feedback-state'; import { PageScreen } from '@/components/page-screen'; import { TextField } from '@/components/text-field'; import { friendlyError } from '@/lib/errors'; import { workspaceService } from '@/services/workspace';
export default function EditProjectScreen() { const { id } = useLocalSearchParams<{ id: string }>(); const router = useRouter(); const [name,setName]=useState(''); const [description,setDescription]=useState(''); const [instructions,setInstructions]=useState(''); const [loading,setLoading]=useState(true); const [saving,setSaving]=useState(false); const [error,setError]=useState<string|null>(null);
  useEffect(()=>{void workspaceService.getProject(id).then(({project:item})=>{setName(item.name);setDescription(item.description??'');setInstructions(item.instructions??'');}).catch(()=>setError('This project could not be loaded.')).finally(()=>setLoading(false));},[id]);
  const save=async()=>{setSaving(true);setError(null);try{await workspaceService.updateProject(id,{name,description,instructions});router.back();}catch(caught){setError(friendlyError(caught,'Project could not be saved.'));}finally{setSaving(false);}};
  return <PageScreen title="Project settings" subtitle="Persistent context for this workspace">{loading?<LoadingState/>:<View style={{gap:16}}><TextField label="Project name" value={name} onChangeText={setName} maxLength={100}/><TextField label="Description" value={description} onChangeText={setDescription} multiline maxLength={1000}/><TextField label="Instructions" value={instructions} onChangeText={setInstructions} multiline maxLength={8000}/>{error?<AppText tone="danger" variant="caption">{error}</AppText>:null}<Button loading={saving} onPress={()=>void save()}>Save changes</Button></View>}</PageScreen>;
}
